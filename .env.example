# Influencelytic Match - Environment Variables Template
# Copy this file to appropriate directories and rename to .env

# ==========================================
# QUICK START - Minimal Required Variables
# ==========================================

# 1. Set this to true to use mock authentication (no API keys needed!)
USE_MOCK_AUTH=true

# 2. Get these from Supabase (free account at supabase.com)
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=your_anon_key_for_frontend
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_for_backend

# 3. Set a secure JWT secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters

# That's it for development! The mock auth system will handle the rest.

# ==========================================
# FULL CONFIGURATION (When Ready for Production)
# ==========================================

# See individual .env.example files in:
# - frontend/.env.example (Frontend variables)
# - backend/.env.example (Backend variables)  
# - ai_service/.env.example (AI service variables)